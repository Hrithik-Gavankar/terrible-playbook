# Archives (not scanned)

APME skips the `archives/` directory when walking the project. Playbooks here
use Galaxy collections for full-rule demos. The default branch layout keeps
`site.yml` and root playbooks **ansible.builtin-only** so portal scans avoid
the ~16% collection-install phase.
