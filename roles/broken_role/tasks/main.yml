---
# L027: This role has no meta/main.yml

- command: echo "I am a broken role task"

- name: Do something risky in the role
  ansible.builtin.shell:
    cmd: rm -rf /tmp/old_data/*
  changed_when: false
  become: true
